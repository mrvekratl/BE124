@model SellerRequestViewModel

<h2>Satıcı Olma Talebi</h2>

<form method="post">
    <div class="form-group">
        <label for="RequestMessage">Talep Mesajı (isteğe bağlı):</label>
        <textarea id="RequestMessage" name="RequestMessage" class="form-control" rows="4"></textarea>
    </div>

    <button type="submit" class="btn btn-primary">Talep Gönder</button>
</form>

